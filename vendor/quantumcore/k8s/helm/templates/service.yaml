apiVersion: v1
kind: Service
metadata:
  name: {{ include "quantumcore-safety-belt.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "quantumcore-safety-belt.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
